#include <iostream>
#include <fstream>
#include <vector>

using namespace std;

int main() {
	int mx;
	int my;
	vector<int> tileMap;

	cout << "Input the dimensions of map: ";
	cin << mx << my;

	for (int x = 0; x < mx; x++) {
		for (int y = 0; y < my; y++) {
			
		}
	}
}
